#ifndef MYMODES_H
#define MYMODES_H

/*
#include "ArMode.h"
#include "ArActionGroups.h"
#include "stdafx.h"

/// Mode for teleoping the robot with joystick + keyboard
class ArModeMyTeleop : public ArMode
{
public:
  /// Constructor
  ArModeMyTeleop(ArRobot *robot, const char *name, char key, char key2);
  /// Destructor
  virtual ~ArModeTeleop();
  virtual void activate(void);
  virtual void deactivate(void);
  virtual void help(void);
  virtual void userTask(void);
protected:
  //ArActionGroupTeleop myGroup;
  // use our new ratio drive instead
  ArActionGroupRatioDrive myGroup;
};
*/
#endif //MYMODES_H